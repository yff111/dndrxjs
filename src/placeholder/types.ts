export type PlaceholderMiddlewareOptions = {
  tagName: string // defaults to 'div'
  className: string // defaults to 'placeholder'
  applyHeight: boolean // if `true` drag-element height will be applied to placeholder
}
